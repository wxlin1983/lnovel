services:
  lnovel:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - lnovel-data:/app/data
    restart: unless-stopped

volumes:
  lnovel-data:
